
<div class="feed">
        <div class="welcome-card">
            <div class="element-box">
                <?php foreach ($this->getData() as $item): ?>
                <div class="element">
                <?= $item ?>
                </div>
                <?php endforeach; ?>
            </div>
        </div>
</div>
