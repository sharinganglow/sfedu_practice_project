<?php
/**
 * @var App\Models\Entity\CategoryModel $model
 */
?>
<div class="main-space">
        <div class="welcome-card">
            <div class="element-box">
                <?php foreach ($model->getData() as $item): ?>
                    <div class="element">
                <?= $this->validateOutput($item['name']) ?>
                    </div>
                <?php endforeach; ?>
            </div>
        </div>
</div>